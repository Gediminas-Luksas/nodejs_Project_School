extends layout
block content
    h2 #{title}
    form(method='POST', action='/events/add')
        #form-grup
            label Hour:
            input.form-control(name='hour' type='text')
        #form-grup
            label Text:
            textarea.form-control(name='title')
        br
        input.btn.btn-primary(type="submit", value='Submit')